export default function SettingsPage() {
  return (
    <section className="card">
      <h3>Settings</h3>
      <p className="muted">Configure API endpoint via <code>VITE_API_BASE_URL</code> and enable OpenAI in backend with <code>OPENAI_API_KEY</code>.</p>
    </section>
  )
}
